# HeroMail Agent 指南

## 项目目标

HeroMail 是邮箱资源池与注册验证码平台。用户选择目标平台，系统分配 Outlook/Hotmail 邮箱，用户用该邮箱完成平台注册并获取验证码；管理员维护邮箱资产、平台规则和订单。

## 必读设计

- 产品设计：`docs/final-product-design.md`
- 服务入口：`cmd/heromail/main.go`
- 领域模型：`internal/domain/models.go`
- 业务存储：`internal/store/store.go`
- HTTP 接口：`internal/http/server.go`
- 前端页面：`internal/web/static/`

## 不可破坏的业务规则

1. 用户选择目标平台，不能输入或选择平台邮箱地址。
2. 邮箱是平台资产，用户不能看到密码、OAuth Token、完整收件箱或其他邮件。
3. 分配单位是“邮箱 × 目标平台”。同一邮箱可以服务不同平台，但同一平台收到验证码后默认进入 `consumed`，不得再次分配。
4. 创建订单预扣余额；分配失败立即不扣款；超时未收到验证码自动退款；收到验证码后默认结算。
5. 订单状态必须通过领域层变更，HTTP 层不能直接修改状态或绕过余额结算。
6. 首版邮箱渠道只有 Outlook 和 Hotmail，新增渠道必须增加适配器和对应测试。

## 技术约束

- 使用 Go；保持 `go test ./...` 和 `go vet ./...` 通过。
- HTTP 层使用 Gin，业务逻辑放在 `internal/store` 或独立领域服务，不在处理器中复制调度规则。
- 前端保持无构建依赖的可运行 MVP；如果引入 Vue/Vite，必须保留现有 API 契约和 Docker 可运行方式。
- 凭证、API Key、完整邮件正文和真实邮箱信息不能进入日志、测试快照或 Git。
- 所有代码注释、提交信息和项目文档使用中文；第三方库名称、协议字段和 API 路径可保留原文。

## 修改流程

1. 先阅读相关领域模型和产品设计，再修改代码。
2. 先写或更新测试，覆盖状态迁移、余额、幂等和权限边界。
3. 运行 `gofmt`、`go test ./...`、`go vet ./...`。
4. 每个提交只包含一个小阶段，提交信息使用中文动词开头，例如“新增订单接口”“补充管理员页面”。
5. 生成图片、Playwright 产物、编译产物和本地配置不要提交；规则见 `.gitignore`。

## API 演示身份

MVP 使用 `X-HeroMail-Role` 和 `X-HeroMail-User` 请求头模拟身份。它只用于本地演示，任何公网部署都必须替换为真实登录、会话和权限系统。

## 安全与合规

只处理已授权邮箱和合规的注册测试。不得增加撞库、绕过第三方风控、批量滥用或隐藏凭证的功能。发现密钥、Token 或个人数据泄露时，立即停止提交并按 `SECURITY.md` 报告。
