# 更新日志

本项目的所有正式版本变更都记录在此文件。版本号遵循语义化版本，发布日期使用 UTC。

## [1.2.1] - 2026-08-18

### 新增

- 公开首页新增浅色实时工作台视觉，以浅灰白背景、深蓝正文、薄荷绿状态色和少量琥珀色提示构成更清晰的产品层次。
- 首页任务控制台改为白色工作面板，保留邮箱分配、事件流、验证码结果和安全交接等真实任务状态。

### 修复

- 修复公开首页深色背景与用户偏好的浅色界面不一致的问题，导航、首屏、流程区、数据边界、行动区和页脚现在使用统一浅色体系。
- 修复浅色主题下移动导航、边框、状态文字和辅助面板对比度不足的问题，并保持桌面与移动端无横向溢出。

### 安全

- 浅色主题继续只展示脱敏任务示例，不暴露邮箱密码、OAuth Token、完整收件箱、库存或内部收码规则。
- 未修改认证、权限、支付、订单、邮箱分配和在线升级安全边界。

### 兼容性

- 本版本仅调整公开页面 CSS，不涉及数据库结构、环境变量、订单状态或 API 契约变更。
- 保留 GSAP `3.15.0`、ScrollTrigger、`prefers-reduced-motion` 和现有响应式断点行为。

### 部署提醒

- 发布或升级前请备份 PostgreSQL；升级后请验证公开首页桌面与移动端、`/readyz`、登录和管理后台入口。
- 正式升级必须等待 GitHub Release 和 `ghcr.io/ljunn/heromail:latest` 多架构镜像发布完成，再从管理员“系统设置”页面点击在线升级。

## [1.2.0] - 2026-08-18

### 新增

- 公开首页升级为深色实时控制台视觉，展示目标平台、邮箱分配、邮件事件流和验证码结果等真实任务节点。
- 首页接入固定版本的 GSAP 与 ScrollTrigger，增加首屏时间线、滚动揭示、低频状态动效和桌面指针反馈。
- 动效支持 `prefers-reduced-motion`，用户启用减弱动态效果后不启动循环动画，核心内容保持立即可读。
- 目标平台编辑弹窗新增 Outlook 和 Hotmail 复选框，可明确配置一个或多个允许的邮箱供应商。

### 修复

- 修复目标平台弹窗在小屏幕上超出视口、底部新建按钮无法点击的问题；表单主体现在独立滚动，提交按钮固定可见。
- 修复发件人域名为空时返回“价格、有效期或允许的邮箱供应商无效”的含混提示，现在按单价、有效期、供应商和发件人域名分别返回准确错误。
- 修复目标平台代码、供应商和规则列表没有统一标准化的问题，保存时会去除空白、转为小写并去重。

### 安全

- 服务端将目标平台允许的邮箱供应商限制为 Outlook 和 Hotmail，拒绝未实现适配器的渠道配置。
- 继续要求至少一个发件人域名，避免空规则接收并解析与目标平台注册无关的邮件。
- 公开服务 DTO 和首页预览不暴露邮箱密码、OAuth Token、完整收件箱、库存或内部收码规则。

### 兼容性

- 不涉及数据库结构、环境变量、订单状态或公开 API 响应结构变更；已有合法目标平台配置继续兼容。
- 目标平台代码保存后统一为小写；依赖大小写不同代码的非标准配置需要在升级后检查。
- GSAP 使用固定的 `3.15.0` CDN 资源；资源不可达时页面保留完整静态内容，仅停用增强动效。

### 部署提醒

- 升级前请备份 PostgreSQL；升级后请验证 `/readyz`、登录、目标平台新增弹窗、列表分页、申请邮箱和支付回调配置。
- 正式升级必须等待 GitHub Release 和 `ghcr.io/ljunn/heromail:latest` 多架构镜像发布完成，再从管理员“系统设置”页面点击在线升级。

## [1.1.1] - 2026-08-18

### 修复

- 修复支付宝官方支付无法读取支付宝控制台常见裸 Base64 应用私钥的问题。
- 支付宝应用私钥现在兼容裸 Base64、PKCS#1 PEM 和 PKCS#8 PEM 格式。

### 兼容性

- 不涉及数据库结构、环境变量和支付回调协议变更；现有支付宝 PEM 配置继续兼容。

### 部署提醒

- 升级前请备份 PostgreSQL；升级后请验证 `/readyz`、登录、充值订单创建和支付宝异步回调配置。

## [1.1.0] - 2026-08-18

### 新增

- 新增无需登录即可访问的公开官网、价格页、API 文档、服务状态和开源部署页。
- 新增公开目标平台价格接口，使用服务端分页并仅返回面向用户的脱敏字段。
- 新增独立用户门户入口 `/app/*`，提供申请邮箱、当前任务、订单、余额、开发者和账户导航；移动端增加底部导航。
- 新增独立管理员后台入口 `/admin/*`，保留侧栏和高密度运营视图。
- 统一 HeroMail 品牌标志、favicon、公开页面预览和工作台品牌资产。

### 修复

- 修复用户登录后仍被组织成管理员后台的问题，公开站、用户门户和管理后台现在按路径和角色分流。
- 修复移动端首页产品预览与服务状态信息重叠的问题。
- 修复公开服务列表可能暴露库存、收码规则和内部平台占用字段的问题。

### 安全

- 未登录公开接口不返回邮箱地址、库存数量、发件人域名、主题关键词、验证码正则、凭证或内部状态。
- 普通用户访问 `/admin/*` 会被拒绝并回到用户门户；管理员切换用户门户仍沿用 Bearer 会话权限。

### 兼容性

- 新增公开页面和工作台深层路由，不改变现有登录、订单、支付、Webhook 和管理员 API 契约。
- `GET /api/v1/public/services` 支持统一 `page`、`page_size` 分页参数；原有登录后 `/api/v1/services` 继续返回管理所需字段。
- 不涉及数据库结构变更，旧版本实例可以从 `v1.0.6` 直接在线升级。

### 部署提醒

- 发布前请备份 PostgreSQL；升级仍必须先创建 GitHub Release，再在管理后台检查更新并点击在线升级。
- 升级完成后请访问公开首页、`/pricing`、`/docs`、`/login`、`/app`、`/admin`，并验证 `/readyz`、申请邮箱和分页列表。

## [1.0.6] - 2026-08-18

### 新增

- 支付管理新增服务商编辑和删除操作，并为易支付、支付宝官方提供独立配置弹窗。
- 易支付表单支持 API 地址、PID、PKey 和可选支付宝通道 ID；支付宝官方表单内置官方生产网关，并分别配置 AppID、应用私钥和支付宝公钥。
- 管理员与普通用户均可在账户设置中验证当前密码后修改登录密码，修改成功后自动撤销全部旧会话。
- 重新设计 HeroMail 标志、站点图标、侧栏图标和平台图标，统一使用清晰的线性视觉体系。
- 系统设置新增主动检查 GitHub 正式版本入口。

### 修复

- 修复支付服务商只能新增、无法编辑和删除的问题。
- 修复两种支付协议共用一组含混字段，容易把易支付商户密钥与支付宝 RSA 密钥配置错误的问题。

### 安全

- 编辑支付服务商时不向浏览器返回已有密钥正文，敏感字段留空可保留服务器端加密值。
- 存在待支付订单时禁止删除对应服务商，避免异步回调失去验签配置。
- 修改密码必须校验当前密码，并在同一事务中更新密码、撤销旧会话和签发新会话。
- 在线升级继续要求管理员登录和权限校验，移除重复的独立升级令牌，同时保持固定官方镜像、版本校验和审计记录。

### 兼容性

- 不涉及数据库结构变更；`HEROMAIL_UPDATE_TOKEN` 已废弃，旧环境中残留的变量不会再被读取。
- 支付服务商 API 新增单项详情读取接口，原有列表、保存和删除接口保持兼容。

### 部署提醒

- 已安装 `v1.0.5` 的实例请先备份 PostgreSQL，再通过管理后台在线升级到本版本。
- 升级后请检查支付管理的两类配置弹窗、管理员账户入口、系统设置检查更新和 `/readyz`。

## [1.0.5] - 2026-08-17

### 修复

- 修复 CDN 或浏览器长期缓存旧版 JavaScript，导致升级后管理后台继续运行旧前端逻辑的问题。
- HTML 中的 CSS 和 JavaScript 地址加入提交级版本标识，每个正式版本使用独立缓存键。
- 入口页面禁止缓存，静态资源要求重新验证，确保网页刷新后立即加载当前发布版本。

### 测试

- 新增静态资源版本标识和 HTTP 缓存响应头的回归测试。

### 兼容性

- 不涉及数据库结构、环境变量和 API 变更，与 `v1.0.4` 完全兼容。

### 部署提醒

- 已安装 `v1.0.4` 的实例请通过管理后台在线升级到本版本。
- 升级完成后刷新“系统设置”，按钮应显示“已是最新版本”并处于禁用状态。

## [1.0.4] - 2026-08-17

### 修复

- 修复管理后台版本号带有或不带 `v` 前缀时的比较问题。
- 当前版本与 GitHub 正式版本一致时，升级按钮稳定显示“已是最新版本”并保持禁用。

### 兼容性

- 不涉及数据库结构、环境变量和 API 变更，与 `v1.0.3` 完全兼容。

### 部署提醒

- 已安装 `v1.0.3` 的实例请继续通过管理后台在线升级到本版本。
- 升级完成后请在“系统设置”确认当前版本为 `v1.0.4`，并检查按钮已禁用。

## [1.0.3] - 2026-08-17

### 修复

- 当前版本与 GitHub 最新正式版一致时，管理后台改为显示“已是最新版本”并禁用升级按钮。
- 升级接口新增同版本校验，即使绕过前端也不会重复重建已是最新版的容器。
- GitHub Release 创建改为草稿、资产上传、正式发布三阶段重试，降低 GitHub API `503` 对发布的影响。

### 兼容性

- 不涉及数据库结构和环境变量变更，与 `v1.0.2` 完全兼容。

### 部署提醒

- `v1.0.2` 实例继续使用管理后台在线升级，升级完成后按钮应处于禁用状态。

## [1.0.2] - 2026-08-17

### 修复

- 正式镜像内嵌版本改为对应的 `v*` Release 标签，不再显示为提交 SHA。
- 修复管理后台将已安装正式版本误判为可升级，导致同一版本可重复触发升级的问题。

### 兼容性

- 不涉及数据库结构、环境变量和 API 变更，与 `v1.0.1` 完全兼容。

### 部署提醒

- `v1.0.1` 实例应在管理后台使用在线升级按钮升级，不得手动替换镜像。
- 升级后“当前版本”应显示 `v1.0.2`，并且在已是最新版时禁用升级按钮。

## [1.0.1] - 2026-08-17

### 修复

- 正式发布任务改为独立交叉编译 amd64/arm64 安装包，避免 GitHub Artifact 短时下载故障阻断 Release。

### 发布

- 日常提交仍保留可下载的构建制品，正式 Release 使用同一源码和版本信息现场构建。
- Release 页面继续强制使用本章内容作为正式更新日志。

## [1.0.0] - 2026-08-17

### 新增

- 完成用户注册、登录、会话、角色权限、个人设置和 API Key 管理。
- 完成 PostgreSQL 持久化、Redis 分配锁、事务化邮箱分配和服务端分页。
- 完成邮箱池、Outlook/Hotmail OAuth2、Microsoft Graph 收信、邮件去重和验证码规则匹配。
- 完成注册订单预扣、提交、收码、完成、取消、超时退款和资金流水链路。
- 完成易支付 MD5 签名与支付宝官方 RSA2 支付，支持回调验签和幂等入账。
- 完成 Webhook HMAC 签名、内网地址防护、指数退避、投递记录和手动重试。
- 完成用户端与管理端，覆盖资源、平台、规则、订单、用户、资金、支付、告警和审计。
- 新增 Docker 一键部署、GitHub Actions amd64/arm64 打包、GHCR 多架构镜像和网页在线升级。

### 安全

- 邮箱凭证、OAuth Token、支付私钥和 Webhook 密钥使用 AES-256-GCM 加密存储。
- API Key 和会话令牌仅保存哈希，明文密钥只显示一次。
- 在线升级固定使用 `ghcr.io/ljunn/heromail:latest`，该标签只在 GitHub 正式 Release 中更新。

### 部署提醒

- 首次启动必须配置强管理员密码、64 位十六进制加密主密钥、Worker 令牌和升级令牌。
- Microsoft Graph 和支付通道需要在管理端配置合法的第三方凭证后才会启用。
