# 更新日志

本项目的所有正式版本变更都记录在此文件。版本号遵循语义化版本，发布日期使用 UTC。

## [1.10.34] - 2026-08-27

### 新增

- 收码 Worker 增加按活跃订单筛选的邮箱凭证分页查询。

### 修复

- 修复 Worker 每轮扫描全部已验证邮箱并逐个查询订单的问题，避免大规模邮箱池产生无效数据库查询、连接耗尽和 `cannot assign requested address` 错误。
- 空闲邮箱不再触发 OAuth 刷新或 IMAP 收件请求，只有存在未过期 `assigned`/`waiting_code` 订单的邮箱进入实时收码流程。

### 安全

- 仅收窄实时收码候选范围，不改变邮箱凭证加密、订单权限和验证码匹配规则。

### 兼容性

- 不需要数据库结构迁移；保留现有订单状态、邮箱状态和环境变量配置。

### 部署注意事项

- 发布后请确认 `/readyz` 返回 `status=ready`，并观察 PostgreSQL 连接错误和应用 CPU 是否回落。

## [1.10.33] - 2026-08-27

### 修复

- Microsoft IMAP OAuth 失败信息不再附带误导性的“缺少 Graph OAuth 凭证”，便于区分真实 IMAP 授权错误。

### 兼容性

- 仅调整错误展示；IMAP OAuth、Graph OAuth 和密码登录行为保持不变。

### 安全

- 不改变凭证处理、日志脱敏和重试策略。

### 部署注意事项

- 发布 `v1.10.33` 后，新产生的 IMAP OAuth 验证错误将直接显示 IMAP 原因。

## [1.10.32] - 2026-08-27

### 新增

- Microsoft IMAP `authenticated but not connected` 失败支持 30 分钟冷却后的自动重试。

### 修复

- 临时 IMAP 会话限制不再永久阻断邮箱重新验证，限流窗口结束后会自动回到验证队列。

### 安全

- 自动重试仅匹配 Microsoft IMAP 明确的临时会话错误，不重试 `invalid_grant` 等授权失效错误。

### 兼容性

- 保留现有验证队列、Redis 去重和低并发限制；其他网络错误仍按原有 5 分钟策略重试。

### 部署注意事项

- 发布 `v1.10.32` 后，已失败的 Microsoft IMAP 临时会话会在 30 分钟后自动重试。

## [1.10.31] - 2026-08-27

### 新增

- Microsoft IMAP 验证默认并发调整为 4，与源项目的验证线程数一致。

### 修复

- 降低批量 OAuth IMAP 重验证触发 Microsoft 会话限制并产生 `authenticated but not connected` 假失败的概率。

### 安全

- 仅调整验证任务并发，不改变邮箱凭证、授权范围或邮箱分配规则。

### 兼容性

- 可通过 `HEROMAIL_VERIFICATION_CONCURRENCY` 显式覆盖验证并发；默认值从 64 调整为 4。

### 部署注意事项

- 发布 `v1.10.31` 后，未完成的邮箱验证队列会以低并发继续处理。

## [1.10.30] - 2026-08-27

### 新增

- 对旧版 Microsoft IMAP `NOOP` 健康检查产生的假失败执行一次性重新验证。

### 修复

- Microsoft IMAP 验证改为认证后直接只读打开 `INBOX`，兼容源项目的实际连接顺序。

### 安全

- 假失败迁移仅匹配明确的 Microsoft IMAP 会话错误，不改变真实授权失效记录。

### 兼容性

- 保留 Microsoft IMAP OAuth、Graph OAuth 和密码登录路径；验证码读取继续使用已验证邮箱。

### 部署注意事项

- 发布 `v1.10.30` 后，服务启动时会将旧版 `NOOP` 假失败记录一次性重新排队验证。

## [1.10.29] - 2026-08-27

### 新增

- 兼容源项目的 `clientId`、`refreshToken`、`accessToken` 和 `imapUser` 导入字段。
- 对历史误判为失败的 Microsoft IMAP OAuth 邮箱执行一次性重新验证。

### 修复

- Microsoft IMAP OAuth 凭证直接使用 IMAP XOAUTH2，不再先用 IMAP Token 请求 Graph。
- IMAP 登录优先使用导入记录中的用户名，兼容源项目的实际邮箱登录方式。

### 安全

- 历史重验证只针对包含 Microsoft IMAP OAuth 凭证的邮箱，凭证仍加密存储且不写入日志。

### 兼容性

- 保留现有 Graph OAuth、密码 IMAP 和邮箱分配规则；无效凭证仍会标记为异常。

### 部署注意事项

- 发布 `v1.10.29` 后，服务启动时只执行一次历史 Microsoft IMAP OAuth 重新验证排队。

## [1.10.28] - 2026-08-27

### 新增

- 分配锁竞争增加短暂重试和独立错误码，避免把并发请求误报为库存耗尽。

### 修复

- 使用原生构建平台交叉编译 Go 二进制，避免多架构镜像构建通过 QEMU 编译导致耗时过长。
- 自动补齐目标平台允许但历史配置缺失的邮箱类型单价，例如 `outlook_de`。

### 安全

- 分配仍使用 Redis 分布式锁、数据库行锁和事务边界，不放宽邮箱验证或平台占用条件。

### 兼容性

- 并发分配繁忙时返回 `429 allocation_busy`；真实无库存仍返回 `503 no_mailbox_available`。
- 保留现有 PostgreSQL、Redis、GHCR 和 Release Action 部署方式。

### 部署注意事项

- 发布 `v1.10.28` 后，GitHub Release Action 会自动备份数据库、切换官方镜像并调用 `/opt/heromail/scripts/upgrade.sh`。

## [1.10.27] - 2026-08-27

### 新增

- Release Action 将校验后的升级脚本安装到服务器固定路径后执行。

### 修复

- 修复自动升级没有调用 `/opt/heromail/scripts/upgrade.sh` 的发布链路不一致问题。

### 安全

- 保持升级脚本 SHA-256 校验和 SSH Secrets 部署认证不变。

### 兼容性

- 保留管理员后台在线升级接口作为兼容入口。

### 部署注意事项

- 发布 `v1.10.27` 后，Release Action 会自动通过 SSH 升级生产环境。

## [1.10.26] - 2026-08-27

### 修复

- 将 GitHub Actions 自动升级的容器健康等待窗口延长到 10 分钟，覆盖存量邮箱迁移和首次启动耗时较长的生产环境，避免服务已能启动却被过早回滚。

### 兼容性

- 保留 `v1.10.25` 的自动 SSH 升级、PostgreSQL 备份、版本校验和失败回滚行为。

### 部署注意事项

- `v1.10.26` 发布后会重新执行完整 Release、镜像构建和 SSH 自动升级；升级期间公网可能短暂返回 502，完成后以 `/readyz` 和容器版本为准。

## [1.10.25] - 2026-08-27

### 新增

- 正式 Release 完成后由 GitHub Actions 通过 SSH 自动调用线上升级脚本，无需管理员打开网页手动触发。
- Release 附带带校验和的 `upgrade.sh`，支持 PostgreSQL 备份、官方 GHCR 版本镜像切换和失败回滚。

### 修复

- 修复发布完成后线上仍停留在旧镜像、必须依赖后台按钮才能升级的问题。
- 修复 Docker 升级没有确认目标版本和健康状态的问题。

### 安全

- Action 只下载正式 Release 的升级脚本并校验 SHA-256，升级脚本只接受 `v*.*.*` 版本和官方 GHCR 镜像。
- SSH 密钥仅通过 GitHub Secrets 注入；备份文件权限限制为管理员可读，日志不输出环境变量或数据库凭据。

### 兼容性

- 保留管理员后台在线升级接口；已有 Docker Compose、PostgreSQL、Redis 和升级执行器部署可直接使用 Action 自动升级。

### 部署注意事项

- 在仓库 Secrets 配置 `SERVER_HOST`、`SERVER_USER`，以及 `SERVER_SSH_KEY` 或 `SERVER_PASSWORD`；可选配置 `SERVER_PORT` 和 `SERVER_APP_HOME`。
- Release 工作流会在正式版本创建完成后自动备份 PostgreSQL、切换镜像并检查 `/readyz`；失败时自动恢复上一镜像。

## [1.10.24] - 2026-08-27

### 新增

- Microsoft 邮箱验证支持 Graph 与 IMAP OAuth 双通道探测，验证成功后保存实际可用的登录方式。
- 导入记录保留 OAuth 协议标记，后续收码 Worker 按已验证的连接方式读取邮箱。

### 修复

- 修复源系统导出的 Outlook `client_id + refresh_token` 被错误当作 Graph 凭据，导致邮箱全部验证失败、订单无法收码的问题。
- 修复 Outlook IMAP OAuth 刷新未使用 `consumers` 端点和 IMAP 权限范围的问题。
- 启动时回填旧版本导入的 Microsoft OAuth 记录并重新加入验证队列，避免存量邮箱停留在 `auth_error` 状态。

### 安全

- Graph 与 IMAP Token 按授权资源隔离，验证失败不会把错误 Token 继续交给另一种协议使用。
- OAuth 刷新和迁移审计只记录资源 ID 与操作结果，不记录邮箱密码、Token 或邮件内容。

### 兼容性

- 保留现有 Graph OAuth、密码 IMAP、邮箱导入格式和订单状态机；无需手工修改数据库表结构。
- 已有 PostgreSQL、Redis 和 `ghcr.io/ljunn/heromail:latest` 部署可直接在线升级。

### 部署注意事项

- 发布后必须等待 GitHub Actions 完成检查、镜像构建和正式 Release，再通过管理员后台在线升级到 `v1.10.24`。
- 升级后检查邮箱池中待验证、授权异常和已验证数量，确认存量 OAuth 邮箱重新进入验证队列，并用新订单验证验证码收取。

## [1.10.23] - 2026-08-26

### 新增

- 服务启动和定时任务增加未验证邮箱订单回收，自动释放坏租约并退还余额。

### 修复

- 修复邮箱在订单分配后验证失败时仍保持收码中、继续占用库存的问题；验证失败会在同一事务内退款并释放邮箱×平台状态。
- 修复收码 Worker 读取到验证状态已失效的邮箱的问题，只有当前仍为 `verified` 的邮箱才会参与收码。

### 安全

- 未通过连接验证的邮箱不会继续对外作为可用库存，也不会向新用户分配。
- 验证失败导致的自动退款写入资金流水和审计日志，便于追踪和对账。

### 兼容性

- 不新增数据库表或字段；现有 PostgreSQL、Redis 配置可直接升级，存量坏租约会在启动时自动清理。

### 部署注意事项

- 发布后必须等待 GitHub Actions 完成检查、镜像构建和正式 Release，再通过管理员后台在线升级到 `v1.10.23`。
- 升级后检查管理员订单页不再出现绑定 `failed`/`pending_verification` 邮箱的进行中订单，并确认余额流水已产生退款记录。

## [1.10.22] - 2026-08-26

### 新增

- 收码 Worker 增加收码失败日志，管理员可从服务日志定位订单落库失败和邮件去重写入失败。

### 修复

- 修复验证码邮件在订单收码落库失败前就写入去重记录的问题；瞬时数据库错误、并发状态冲突或队列异常后，邮件会在下一轮继续尝试收码。
- 修复收码失败被静默忽略的问题，避免线上看起来“邮件已到达但订单始终没有验证码”。

### 安全

- 日志只记录订单、邮箱资源和邮件消息标识，不记录验证码、邮箱密码、OAuth Token 或邮件正文。

### 兼容性

- 不改变订单状态、余额结算和邮件去重数据结构；无需数据库迁移，现有 PostgreSQL、Redis 配置可直接升级。

### 部署注意事项

- 发布后必须通过管理员后台在线升级到 `v1.10.22`，线上运行旧镜像不会包含本修复。
- 升级后检查公开状态为 `mail_worker: operational`，并在管理员订单页确认新订单能进入“收码中”后收到验证码。

## [1.10.21] - 2026-08-26

### 新增

- 管理员可对 Microsoft 邮箱发起绑定目标邮箱的 OAuth 重新授权，授权完成后自动重新排队验证。
- 邮箱历史平台注册扫描使用独立 Redis 队列，验证连接和历史扫描不再互相阻塞。

### 修复

- 修复内存存储订单分配未检查邮箱验证状态的问题，待验证和验证失败邮箱不会再被分配。
- 修复 Graph OAuth 失效时将失效 access token 当作 IMAP 凭证重试的问题；只有独立密码凭证才允许 IMAP 回退。
- 修复暂时性网络、连接和服务端错误不会重新进入验证队列的问题，并缩短队列补偿间隔。
- 修复公开状态接口始终显示 mail worker 正常的问题。

### 安全

- OAuth 重新授权状态绑定目标邮箱，并校验授权返回账号与目标邮箱一致，避免将错误账号凭证写入邮箱资源。
- 订单分配、库存统计和收码资源继续只接受已验证邮箱；新增回归测试覆盖未验证邮箱分配边界。

### 兼容性

- 保留原有 Microsoft OAuth、密码 IMAP、邮箱导入和管理员验证接口；新增 Redis 历史扫描队列键，不需要数据库迁移。
- 未配置并发环境变量时，验证和历史扫描默认并发调整为 64 和 8；已有环境变量仍优先使用。

### 部署注意事项

- 发布后必须等待 GitHub Actions 完成检查、镜像构建和正式 Release，再通过管理员后台在线升级。
- 升级后请在管理员“邮箱池”页面检查待验证、授权异常和已验证数量；Graph 失效且没有邮箱密码的账号需要点击“重新授权”。

## [1.10.20] - 2026-08-26

### 新增

- 新增公开的隐私政策和服务条款页面，可直接用于 Google OAuth 同意屏幕的公开链接。
- 官网页脚、注册页和管理员 Google OAuth 配置卡新增隐私政策与服务条款入口。

### 修复

- 修复 Google OAuth 配置说明缺少应用隐私权政策和服务条款地址的问题。

### 安全

- 公开政策明确邮箱凭证、OAuth Token、完整收件箱和跨平台邮件隔离边界，不新增敏感数据返回。

### 兼容性

- 保留原有公开路由、用户门户、管理员后台和 OAuth 配置接口；新增 /privacy 与 /terms 公开路由。

### 部署注意事项

- Google Cloud OAuth 同意屏幕可填写 https://heromail.cc/privacy 和 https://heromail.cc/terms；自定义域名部署时请替换为实际公开域名。

## [1.10.19] - 2026-08-26

### 修复

- 修复 Google OAuth 配置卡片在接入渠道页面位置不当且重复显示的问题。

### 兼容性

- 保留 Google OAuth 配置接口、加密存储和现有 OAuth 授权流程。

### 部署注意事项

- 升级后从“接入渠道”页面标题下方配置 Google OAuth，旧的重复展示卡片会自动隐藏。

## [1.10.18] - 2026-08-26

### 新增

- 管理员“接入渠道”新增 Google OAuth 客户端配置表单，可直接填写客户端 ID、客户端密钥和回调地址。
- Google OAuth 配置加密保存到 PostgreSQL，保存后立即生效，无需修改服务器 `.env` 或重启服务。

### 修复

- 修复线上只有“连接 Google 邮箱”按钮、没有 Google OAuth 配置入口的问题。

### 安全

- 客户端密钥使用现有 AES-256-GCM 主密钥加密保存，读取接口只返回配置状态、脱敏客户端 ID 和回调地址。
- Google OAuth 配置变更写入管理员审计日志，客户端密钥不进入响应或审计详情。

### 兼容性

- 保留 `GOOGLE_CLIENT_ID`、`GOOGLE_CLIENT_SECRET`、`GOOGLE_REDIRECT_URI` 环境变量；数据库配置存在时优先使用管理员保存的配置。

### 部署注意事项

- 升级后在“接入渠道”填写 Google Cloud Web 应用客户端信息，回调地址使用 `https://heromail.cc/api/v1/oauth/google/callback`。

## [1.10.17] - 2026-08-26

### 新增

- Google Gmail OAuth 启动按钮增加授权准备中的状态反馈。

### 修复

- 修复 Google OAuth 尚未配置或启动失败时前端只记录控制台错误、管理员点击后看不到任何反馈的问题。
- 修复授权地址缺失时按钮无法恢复的问题。

### 安全

- 不改变 Google OAuth 凭证仅在服务端使用、Token 加密保存的边界。

### 兼容性

- 保留现有 Google、Microsoft OAuth 接口和管理员接入渠道页面。

### 部署注意事项

- 生产环境仍需配置 \`GOOGLE_CLIENT_ID\`、\`GOOGLE_CLIENT_SECRET\` 和 \`GOOGLE_REDIRECT_URI\` 后才能跳转 Google 授权页面。

## [1.10.16] - 2026-08-26

### 新增

- 管理员侧栏新增“接入渠道”入口，Google Gmail OAuth 配置页不再是隐藏路由。

### 修复

- 修复 Google OAuth 接入页面虽已实现但管理员导航无法进入的问题。

### 安全

- 不改变 OAuth Token 的加密保存和服务端使用边界。

### 兼容性

- 保留现有管理员路由和 Microsoft、Google OAuth 接入接口。

### 部署注意事项

- 升级后可从管理员侧栏“接入渠道”进入 Google Gmail OAuth 配置和授权入口。

## [1.10.15] - 2026-08-26

### 新增

- 管理员可通过 Google OAuth 在官方页面完成 Gmail 登录和 2FA，授权后的 Gmail 自动加入邮箱池并排队验证。
- Gmail OAuth Token 过期时自动刷新，验证和收件任务继续使用 Gmail IMAP XOAUTH2。

### 修复

- Google OAuth 回调增加一次性状态和邮箱归属校验，避免跨授权流程复用状态或接入非 Gmail 地址。

### 安全

- Google Client Secret、Access Token 和 Refresh Token 仅通过服务端环境变量和加密存储使用，不进入前端响应、日志或审计详情。
- OAuth 授权在 Google 官方页面完成，HeroMail 不接收 Google 密码或 2FA 地址。

### 兼容性

- 保留 Microsoft Graph OAuth 和 Gmail/iCloud/Mail.com IMAP 应用专用密码接入；原有邮箱导入格式不变。

### 部署注意事项

- 在 Google Cloud 将 `https://heromail.cc/api/v1/oauth/google/callback` 配置为授权重定向 URI，并在服务环境配置 `GOOGLE_CLIENT_ID`、`GOOGLE_CLIENT_SECRET`、`GOOGLE_REDIRECT_URI`。
- Gmail OAuth 全量邮件权限为 `https://mail.google.com/`；公开应用须按 Google 要求配置测试用户并完成必要审核。

## [1.10.14] - 2026-08-26

### 新增

- 邮箱池新增文本粘贴导入入口，粘贴内容与 TXT、CSV、JSON Lines 文件使用同一套逐行导入和验证流程。
- Gmail 导入格式支持“邮箱----密码----2FA 地址”，例如 `https://2fa.live/...`；2FA 地址会作为独立凭证字段保存。

### 修复

- 修复 2FA 地址被误识别为 Microsoft refresh token 的问题。
- 修复邮箱导入按钮在成功刷新或请求失败后仍显示忙碌状态的问题。

### 安全

- 2FA 地址与邮箱密码一样加密保存，不会返回邮箱列表、错误提示或日志；系统不会主动访问第三方 2FA 地址。

### 兼容性

- 保留现有 TXT、CSV、JSON Lines 和分隔符导入格式；Gmail 收件支持应用专用密码或 OAuth，普通 Google 密码和 2FA 地址不替代 IMAP 凭证。

### 部署注意事项

- 升级后在“接入渠道”配置 Google OAuth 并在“邮箱池”使用粘贴导入；大批量粘贴仍由服务端按行读取，不会一次性加载整批记录。

## [1.10.13] - 2026-08-25

### 新增

- 开发者密钥列表显示有效/已吊销状态，创建后的完整密钥在弹窗关闭前支持重复复制。
- API 鉴权兼容 `X-API-Key` 请求头，便于不支持 Bearer 输入的客户端接入。

### 修复

- 修复开发者密钥吊销按钮无反馈的问题，增加确认、进行中状态、失败提示和成功刷新。
- 修复创建 API Key 时刷新顺序不当导致一次性密钥弹窗体验不稳定的问题。
- API 文档明确区分完整密钥与列表前缀，避免使用前缀导致“API Key 无效或已被撤销”。

### 安全

- API Key 仍仅保存 SHA-256 哈希；弹窗关闭后无法恢复旧密钥明文，已吊销密钥立即失效。

### 兼容性

- 保留现有 `Authorization: Bearer hm_...` 鉴权方式；新增 `X-API-Key: hm_...` 兼容方式不改变权限和吊销规则。

### 部署注意事项

- 升级后请在用户门户重新创建并保存需要继续使用的密钥；列表中的 `hm_...` 前缀不能作为完整密钥调用接口。

## [1.10.12] - 2026-08-25

### 修复

- Graph OAuth 失效时，存在邮箱密码或应用专用密码的账号会移除失效 access token，自动优先回退 IMAP；没有 IMAP 凭证或 IMAP 认证失败时展示具体原因。
- 目标平台库存、邮箱类型库存和管理员总览的可用邮箱数只统计已验证邮箱，待验证和验证失败的邮箱不会再计入库存。

### 安全

- Graph 失效回退只使用导入时已保存的密码或应用专用密码，不绕过邮箱认证，也不会把凭证写入错误信息。

### 兼容性

- 保留现有 Graph OAuth、IMAP OAuth 和密码导入格式；仅调整 Graph 失败后的通道选择和库存统计口径。

### 部署注意事项

- 升级后重新验证带密码的 Microsoft 邮箱，确认连接方式显示为 IMAP；只有 refresh token 的邮箱仍需重新授权 Graph 或补充 IMAP 密码/应用专用密码。

## [1.10.11] - 2026-08-25

### 新增

- 邮箱连接验证、历史平台注册扫描和实时收码改为有上限的并发 Worker，默认分别为 32、4、32，可通过环境变量调整。

### 修复

- 收码 Worker 不再轮询待验证或验证失败的邮箱，也不再为空闲邮箱重复读取收件箱；只处理已验证且存在活跃订单的邮箱。
- 历史平台注册识别从实时收码循环中分离，避免大批量导入时阻塞验证码收取。

### 安全

- 并发仅由固定上限控制，不改变邮箱×平台原子分配、邮件去重和订单状态事务；每个验证任务仍有超时边界。

### 兼容性

- 保留原有队列、邮箱凭证和数据库结构；未配置并发环境变量时使用默认上限。

### 部署注意事项

- 可按上游 Graph/IMAP 限流能力设置 `HEROMAIL_VERIFICATION_CONCURRENCY`、`HEROMAIL_HISTORY_SCAN_CONCURRENCY` 和 `HEROMAIL_MAIL_POLL_CONCURRENCY`，不建议无限增大。

## [1.10.10] - 2026-08-25

### 修复

- 管理员邮箱列表兼容压缩旧版本保存的 Microsoft Graph 原始授权错误，历史记录不再展示完整 JSON。

### 安全

- 仅在接口返回明确的 `invalid_grant` 或 AADSTS 授权错误时替换展示文案，邮箱凭证和令牌不会进入响应。

### 兼容性

- 不修改邮箱凭证、订单和邮箱平台状态；历史错误只在读取时规范化，新记录继续使用 v1.10.9 的紧凑错误。

### 部署注意事项

- 升级后刷新邮箱池页面，确认旧 Graph 错误显示为重新授权或切换 IMAP的处理提示。

## [1.10.9] - 2026-08-25

### 修复

- 邮箱已切换为 IMAP 后不再重复尝试刷新失效的 Microsoft Graph token，避免后台持续产生 `invalid_grant` 错误。

### 安全

- Graph 与 IMAP 的通道选择仍由邮箱验证状态和已配置凭证决定，不绕过邮箱授权校验。

### 兼容性

- 不修改邮箱凭证格式、数据库结构或现有订单状态；已验证的 IMAP 邮箱继续正常收件。

### 部署注意事项

- 升级后检查 Graph 授权失效邮箱是否已显示可操作提示，并确认带密码的邮箱已稳定回退到 IMAP。

## [1.10.8] - 2026-08-25

### 修复

- Microsoft Graph 刷新 token 时不再重复提交 `scope`，避免未授予权限的 refresh token 触发 `AADSTS70000 invalid_grant`。
- Graph 授权失效后会停止重复轮询并记录可操作提示；存在邮箱密码时自动回退 IMAP，成功后将连接方式切换为 IMAP。
- 管理员错误信息不再展示整段 Microsoft 原始 JSON，改为“重新授权 Graph 或切换 IMAP”的明确处理建议。

### 安全

- refresh token 仍只发送到对应 Microsoft 租户的官方 token 接口；授权失效不会降级为绕过权限的 Graph 请求。

### 兼容性

- 首次 OAuth 授权仍请求 `offline_access Mail.Read User.Read`；仅调整后续 refresh token 请求，已有合法授权继续兼容。

### 部署注意事项

- 发布前创建并校验 PostgreSQL 备份；升级后重新验证一个 Graph 邮箱，确认授权失效时可回退 IMAP，未配置 IMAP 时提示重新授权。

## [1.10.7] - 2026-08-25

### 修复

- 修复支付宝 RSA2 签名串错误包含 `sign_type` 导致真实异步回调验签失败的问题。
- 修复支付平台回调晚于本地 30 分钟回收任务时，已付款订单被拒绝入账的问题；已验签且金额一致的过期订单现在可以完成入账。
- 修复支付通道停用后，已创建订单的合法异步回调被拒绝的问题。
- 用户端将支付订单的 `expired` 状态显示为“已超时”，不再直接显示内部状态值。

### 安全

- 仍要求支付服务商类型匹配、回调签名有效、交易状态成功且回调金额与订单金额完全一致；重复回调继续幂等处理。

### 兼容性

- 不修改支付订单表结构和支付回调 URL；现有待支付、已支付和已过期订单可继续由原回调完成。

### 部署注意事项

- 发布前创建并校验 PostgreSQL 备份；升级后验证支付宝异步回调、支付订单状态和充值资金流水。

## [1.10.6] - 2026-08-25

### 修复

- 修复申请页当前平台库存接口重复执行同一查询的问题，库存总数与邮箱类型明细现在由一次查询同时返回。

### 安全

- 库存接口继续使用平台启用状态、邮箱平台占用、健康度、连接有效期、冷却时间和每日额度约束；本版本仅减少重复读取。

### 兼容性

- 不修改数据库结构和接口响应字段，继续兼容现有用户门户申请流程。

### 部署注意事项

- 发布前创建并校验 PostgreSQL 备份；升级后验证申请页库存、订单列表、钱包和支付配置。

## [1.10.5] - 2026-08-25

### 修复

- 申请页实时库存改为只查询当前选中的目标平台，不再为显示申请页面扫描全部目标平台。
- 切换目标平台后自动刷新该平台库存，其他页面继续保持轻量加载。

### 安全

- 下单仍由服务端在事务和 Redis 锁内重新校验平台、邮箱类型、库存和余额；单平台库存接口不改变分配边界。

### 兼容性

- 不修改数据库结构和已有 API 契约；继续兼容 `/api/v1/services/{code}/availability` 接口。

### 部署注意事项

- 发布前创建并校验 PostgreSQL 备份；升级后验证申请页平台切换、库存刷新、订单列表和钱包页面。

## [1.10.4] - 2026-08-25

### 新增

- 用户申请页支持先加载平台和订单基础信息，再在后台加载实时库存；库存加载期间页面可操作且明确显示当前状态。
- 用户平台列表新增可选的轻量模式，订单筛选只读取平台元数据，不等待库存统计。

### 修复

- 修复用户门户申请页和订单页被大规模邮箱库存统计长期阻塞的问题。
- 修复平台库存接口重复执行同一库存查询的问题，减少一次完整候选邮箱扫描。
- 修复每日额度查询对每个候选邮箱重复扫描邮箱表的问题，改为按邮箱池聚合后复用。
- 修复用户门户页面切换时一直停留在旧页面的问题，先展示目标页面并提供局部刷新状态。
- 修复 Webhook 列表串行请求造成的额外等待，端点和投递记录现在并行加载。

### 安全

- 库存查询仍保留邮箱状态、平台占用、健康分、连接有效期、冷却时间和每日额度约束；本版本只改变查询执行方式。
- 订单分配继续在领域事务和 Redis 锁内执行，不使用轻量平台列表绕过库存校验。

### 兼容性

- `/api/v1/services` 默认响应契约不变；仅支持通过 `availability=false` 请求不含实时库存的用户平台元数据。
- 不修改订单、余额、支付回调和邮件隔离接口；数据库通过现有 GORM 迁移补充邮箱平台状态组合索引。

### 部署注意事项

- 发布前创建并校验 PostgreSQL 备份；升级后验证 `/readyz`、申请页库存异步刷新、订单筛选、钱包、Webhook 和支付配置。
- 首次打开申请页可能先显示“库存读取中”，库存查询完成后会自动更新可选邮箱类型和库存数量。

## [1.10.3] - 2026-08-25

### 新增

- 用户门户按当前页面按需加载数据，钱包、用量、开发者和账户页面不再预加载无关的平台与订单列表。

### 修复

- 修复钱包页首次打开请求链路过长的问题；充值方式和充值记录并行加载，充值方式在短时间内复用缓存。
- 修复应用启动时重复请求当前用户信息的问题，减少首屏等待和页面切换延迟。

### 安全

- 本版本仅调整前端请求调度和短时缓存，不改变认证、支付回调、余额结算或权限校验。

### 兼容性

- 不修改数据库结构和接口契约，继续使用现有服务端分页响应及无构建依赖的静态前端。

### 部署注意事项

- 发布前创建并校验 PostgreSQL 备份；升级后验证 `/readyz`、登录、钱包余额、充值方式和充值记录分页。

## [1.10.2] - 2026-08-25

### 新增

- 用户门户在当前任务、最近订单、订单列表和订单详情中新增“查看本单邮件”入口；用户可以查看属于当前订单目标平台的匹配邮件，并继续使用默认折叠的 HTML 安全预览。
- 管理员邮箱列表新增服务端邮箱地址搜索，搜索结果继续使用统一分页响应，不查询全量数据后再由前端筛选。
- 公开价格页改为按目标平台展示各邮箱类型独立价格，并明确“先选平台、再选邮箱类型、按最终分配类型结算”的三步流程；首页同步更新多渠道、实际类型计费和超时退款文案。

### 修复

- 修复用户订单邮件入口隐藏在详情深处、用户难以发现收件能力的问题。
- 修复管理员目标平台价格项在桌面和移动端拥挤在同一行的问题，改为可扫描的独立价格项。
- 移除公开导航和页脚中的开源部署、GitHub 入口；旧 `/open-source` 路由改为不暴露仓库地址的下线提示。

### 安全

- 用户订单邮件继续校验订单所有权，并按目标平台规则和订单有效期过滤，不能读取其他平台或其他用户的邮件。
- 邮件正文仍只在隔离 sandbox 中按需加载，凭证、Token 和完整邮箱资产不会进入用户接口或审计详情。

### 兼容性

- 不修改数据库结构、订单状态机、支付配置和已有订单；邮箱搜索通过可选 `query` 参数提供，未传参数时保持原有列表行为。
- 保持公开服务 DTO 的脱敏边界、价格字段和统一分页契约；用户订单邮件接口路径保持 `/api/v1/orders/{id}/messages` 不变。
- 继续使用无构建依赖的静态前端，桌面端、移动端和旧公开路由保持可访问。

### 部署注意事项

- 发布前创建并校验 PostgreSQL 备份；升级后验证 `/readyz`、登录、本单邮件隔离、邮箱搜索、公开价格页和支付回调配置。
- 在桌面和 390px 移动视口检查首页、价格页和用户订单入口；确认邮件默认折叠，展开后 HTML 正文正常显示。

## [1.10.1] - 2026-08-25

### 新增

- 用户门户、管理员后台和公开页面统一补充按钮、输入框、表格、状态标签和弹窗的悬停、焦点、选中与禁用反馈。
- 移动端底部导航、弹窗、Toast 和抽屉增加安全区域适配与更明确的层级阴影；长表格表头支持吸顶浏览。

### 修复

- 修复平台卡片、邮箱类型选择和后台侧栏当前项反馈不够明显的问题，空任务和无数据状态增加可识别的边界与下一步提示。
- 修复公开页面与登录页面控件交互反馈不一致的问题，统一移动端触控尺寸并降低重复点击造成的误操作风险。

### 安全

- 本版本仅调整前端视觉和交互样式，不改变认证、权限、邮箱凭证、订单状态机、支付回调或邮件隔离逻辑。
- 保持可见焦点环、键盘操作和减少动效偏好，避免优化样式影响辅助操作与敏感管理动作确认。

### 兼容性

- 不修改数据库结构、环境变量、API 契约、订单数据和支付配置，可从 `v1.10.0` 直接在线升级。
- 继续使用无构建依赖的静态前端，桌面端与移动端保持现有路由和分页行为。

### 部署注意事项

- 发布前创建并校验 PostgreSQL 备份；升级后验证 `/readyz`、登录、服务与订单分页、申请邮箱和支付回调配置。
- 升级后在桌面和移动视口各检查一次用户门户、管理员后台、公开首页和登录页面的布局与交互反馈。

## [1.10.0] - 2026-08-25

### 新增

- 目标平台改为按邮箱类型分别定价；Outlook、Outlook.de、Hotmail、Gmail、iCloud 和 Mail.com 可独立启停并设置价格。
- 用户下单新增必填 `mailbox_providers` 数组，可同时选择一个或多个可接受的邮箱类型；订单持久化请求类型、实际分配类型和实际成交价格。
- 登录后的服务列表与库存接口新增 `provider_prices` 和 `available_by_provider`，公开价格接口新增脱敏的 `provider_prices`；用户门户、公开价格页、管理员后台与 API 文档同步展示逐类型价格。

### 修复

- 修复平台统一单价无法反映不同邮箱渠道成本的问题；余额校验按所选类型最高价格执行，最终只按实际分配类型扣费。
- 修复申请页切换平台或选择邮箱类型时交互反馈不清晰的问题；选择过程现在只局部更新配置区，不重新加载页面或重复请求接口。
- 修复管理员目标平台价格表在移动端需要横向滚动的问题，改为逐平台摘要布局并完整展示各邮箱类型价格。

### 安全

- 服务端严格校验下单邮箱类型必须受支持、由目标平台允许且已配置非负价格，客户端不能绕过类型白名单或提交具体邮箱地址。
- 邮箱分配、实际价格扣款、订单类型快照、邮箱平台租约和资金流水继续位于同一事务；生产分配继续使用 Redis 跨进程锁。
- 公开服务 DTO 只增加邮箱类型价格，不返回库存、邮箱地址、发件人域名、主题关键词、正则、凭证或平台占用状态。

### 兼容性

- PostgreSQL 启动迁移会把旧平台单价复制到该平台当时允许的每种邮箱类型，并从已有订单邮箱补齐实际类型与请求类型；现有订单成交价、余额、支付配置和平台注册状态保持不变。
- 数据库保留旧单价列并同步写入逐类型最低价，便于必要时回滚旧版本；新版本运行时只使用逐类型价格完成校验和扣费。
- 创建订单 API 不再接受缺少 `mailbox_providers` 的请求，服务列表不再返回平台级 `price`；API 客户端必须先读取 `provider_prices` 并提交至少一个邮箱类型。

### 部署注意事项

- 发布前创建并校验 PostgreSQL 备份；在线升级会自动执行数据迁移，禁止在升级过程中手工修改平台价格或订单数据。
- 升级后逐个平台检查六种邮箱类型的价格；迁移初始值沿用该平台升级前单价，管理员可再按真实渠道成本分别调整。
- 升级后验证 `/readyz`、登录、服务与订单分页、逐类型下单和实际扣费，并确认现有支付回调配置未丢失。

## [1.9.0] - 2026-08-25

### 新增

- 邮箱池新增 Gmail、iCloud 和 Mail.com 渠道；Mail.com 覆盖官网当前列出的完整邮箱域名集合，三类渠道均通过 TLS IMAP 读取邮件。
- 空库默认目标平台调整为 Adobe、Imagine、Krea、Leonardo、OpenAI、Runway 和 Grok；Grok 使用 `x.ai`、`Validate your email` 与字母数字验证码规则，OpenAI 使用远端注册代码中确认的三组标题关键词。
- 管理员邮箱列表新增“平台注册”入口，可按“邮箱 × 目标平台”手工标记已注册；导入验证仍会自动扫描全部平台历史邮件并写入状态。

### 修复

- 修复非 Microsoft 邮箱可能误用 Microsoft Graph 或 Microsoft Token 刷新流程的问题；Gmail、iCloud 和 Mail.com 现在直接选择各自 IMAP 服务端。
- 修复 Microsoft OAuth 可能接受非 Microsoft 邮箱域名并在后续连接阶段选择错误渠道的问题。
- 移除新安装默认配置中的 GitHub、Discord 和 Telegram，并清理公开页面与 API 示例中的旧平台文案。

### 安全

- 邮箱渠道按精确域名识别，未知域名不允许导入；所有 IMAP 连接固定使用 TLS 1.2 及以上和对应服务端 SNI。
- 手工标记平台注册状态使用数据库行锁和事务，租用中的邮箱平台状态禁止覆盖，实际变更写入管理员审计日志。
- Microsoft Graph Token 仅发送到 Microsoft API；Google、Apple 和 Mail.com 凭证只用于对应 IMAP 连接，凭证仍使用 AES-256-GCM 加密存储。

### 兼容性

- 保持现有 TXT、CSV、JSON Lines 流式导入格式、统一分页响应、订单状态机、自动扣费和超时退款规则不变。
- 不新增 PostgreSQL 表或必填字段；现有邮箱、订单、余额、支付配置和邮箱平台注册状态无需迁移。
- 已有数据库不会自动新增或恢复目标平台，管理员删除或修改的平台在升级后保持原样；新默认平台仅用于空库首次初始化。

### 部署注意事项

- 发布前创建并校验 PostgreSQL 备份；在线升级页面会再次执行升级前备份，禁止绕过网页升级流程。
- 升级后在目标平台页面新增或更新 Adobe、Imagine、Krea、Leonardo、OpenAI、Runway 和 Grok，并删除或停用不再需要的 GitHub、Discord、Telegram。
- 升级后验证 `/readyz`、登录、列表分页和支付回调配置；分别导入一条 Gmail、iCloud 或 Mail.com 测试账号时，应使用已启用 IMAP 的应用专用密码并确认自动历史扫描结果。

## [1.8.8] - 2026-08-24

### 新增

- 用户订单新增相关邮件入口，只展示订单目标平台在租约时间窗口内匹配到的邮件，并保持服务端分页。
- 目标平台支持配置多个主题关键词；收码 Worker 会同时校验发件人域名和任一主题关键词，再按正则从真实邮件中提取验证码。
- 邮件列表默认折叠，首次展开时才加载经过安全处理的 HTML 正文；同一邮箱在同一平台连续五次超时后自动标记为已使用并停止发放。

### 修复

- 移除模拟验证码和用户端“已发送”“完成”“取消”流程；下单后立即扣费并自动开始收码，订单完成、取消和超时退款统一由后台处理。
- 修复验证邮件在订单刚创建时到达可能无法匹配的问题；收码任务现在从下单时开始，固定 30 分钟未收到验证码会自动退款并回收邮箱。
- 修复用户可以在已收到验证码后主动取消并触发退款的问题；验证码已接收状态只允许后台完成归档。
- 修复公开文档仍展示旧的 10 分钟有效期和用户状态变更接口的问题。

### 安全

- 用户查看订单邮件前会校验订单所有权，并同时按平台发件人、主题关键词和订单时间窗口隔离，不能读取其他平台或历史邮件。
- HTML 邮件继续使用无权限 sandbox 展示，移除脚本、表单、事件属性、危险链接和远程跟踪资源。
- 扣费、超时计数、邮箱平台状态、退款流水和订单状态保持在同一事务内；管理员完成、取消及第五次超时停发均写入审计日志。

### 兼容性

- PostgreSQL 启动迁移会自动为邮箱平台状态新增 `timeout_count` 字段，现有记录默认从零开始，无需手工迁移。
- 所有目标平台收码时限统一为 1800 秒；旧数据和公开服务响应会按 30 分钟展示，管理员保存规则时也必须使用该值。
- 移除用户态 `/orders/{id}/submitted`、`/complete` 和 `/cancel` 接口；客户端只需创建订单并轮询详情或 Webhook，管理员改用对应后台接口处理归档和取消。

### 部署注意事项

- 发布前已创建并校验 PostgreSQL 备份；在线升级仍需由版本升级页面再次执行升级前备份，不得绕过网页升级流程。
- 升级后验证 `/readyz`、登录和列表分页，并创建一笔测试订单确认立即扣费、自动收码、平台隔离邮件、30 分钟超时退款及支付回调配置未丢失。

## [1.8.7] - 2026-08-24

### 新增

- 管理员收件箱支持 HTML 阅读和纯文本双模式，每封邮件可独立切换正文显示方式。
- Microsoft Graph 邮件响应补充正文类型；IMAP 回退连接新增 MIME、multipart 和 quoted-printable 正文解析。

### 修复

- 修复 HTML 邮件被放入纯文本区域、直接显示标签源码的问题。
- 修复 IMAP 邮件回退时显示完整 MIME 头、分段边界或编码正文的问题。
- 修复屏蔽远程邮件图片后出现破图图标的问题，改为明确的安全占位提示。

### 安全

- HTML 邮件只在无权限 sandbox iframe 中展示，并移除脚本、表单、嵌入对象、事件属性和危险链接。
- 默认屏蔽远程图片、CSS 外链资源和跟踪像素，避免管理员查看邮件时向第三方泄露访问行为。

### 兼容性

- 收件接口仅新增可选的 `body_type` 字段，保持原有 `body`、`body_preview` 和分页响应兼容。
- 不修改 PostgreSQL 表结构、Redis 数据、订单状态机、邮箱凭证、支付配置和现有目标平台规则。

### 部署提醒

- 升级前由在线升级页面创建并校验 PostgreSQL 备份；升级后在管理员邮箱列表打开一封 HTML 邮件，确认排版、纯文本切换和远程图片屏蔽状态。
- 使用 IMAP 回退的邮箱应额外验证 multipart 与 quoted-printable 邮件正文能够正常阅读和匹配验证码。

## [1.8.6] - 2026-08-24

### 新增

- 管理员邮箱列表新增“收件”入口，可分页查看邮件主题、发件人、时间和正文。
- 收件读取优先使用 Microsoft Graph，Graph 不可用时自动回退 IMAP；Graph 支持沿分页链接读取历史邮件。
- 邮箱入库后的验证任务会扫描历史邮件并匹配全部目标平台规则，命中后自动标记对应的邮箱平台注册状态。

### 修复

- 修复只有活跃订单才扫描收件的问题，导入邮箱中的历史平台注册邮件现在也能参与平台状态识别。
- 修复 Graph 邮件正文未参与验证码规则匹配的问题。

### 安全

- 邮件正文只通过管理员权限的只读接口返回并在页面转义展示，邮箱凭证、Token 和邮件内容不会写入审计详情。
- 邮箱平台注册状态仅在可用状态下原子变更为已注册，并记录状态变更审计。

### 兼容性

- 保持现有 Outlook、Outlook.de 和 Hotmail 导入格式、邮箱唯一池、订单状态机及服务端分页契约不变。
- 旧邮箱适配器仍使用最近邮件读取接口；新增收件查看和历史扫描接口为可选能力，不破坏现有测试适配器。

### 部署提醒

- 升级前按正式流程创建并校验 PostgreSQL 备份；升级后等待验证 Worker 处理导入邮箱，再在管理员邮箱列表点击“收件”确认 Graph/IMAP 读取和平台注册状态。
- 单个邮箱收件扫描最多保留最近 1000 封邮件，避免超大收件箱占用过多服务内存。

## [1.8.5] - 2026-08-21

### 新增

- 申请邮箱任务会根据当前状态显示对应操作提示，收到验证码后直接展示结果，不再保留提交注册邮件的旧引导。

### 修复

- 修复验证邮件在用户点击“我已发送注册邮件”之前已经到达时无法识别、任务一直停留在等待提交步骤的问题；收码窗口现在从邮箱分配时间开始计算。
- 修复申请邮箱页面选择平台、提交任务和轮询状态时整体刷新造成的闪烁、滚动位置变化和输入焦点丢失问题，改为局部更新当前任务区域。
- 修复管理员调整用户余额时必须填写原因的问题；原因改为选填，未填写时使用统一的审计说明。

### 安全

- 收码窗口继续拒绝邮箱分配前已经存在的历史邮件，避免旧验证码被错误关联到新订单。
- 管理员余额调整仍完整记录资金流水和审计日志，说明改为选填不会绕过审计。

### 兼容性

- 不改变订单状态机、余额结算、支付回调和公开 API；现有 PostgreSQL 与 Redis 数据可以直接在线升级。
- 邮箱分配后一分配窗口内提前到达的 Microsoft Graph 邮件可被识别，现有 Outlook、Hotmail 和 Outlook.de 接入无需重新导入。

### 部署提醒

- 升级前由管理员在线升级页面创建并校验 PostgreSQL 备份；升级后验证提前到达验证码可自动展示、申请页交互不再整页刷新，以及余额调整说明可以留空。

## [1.8.4] - 2026-08-21

### 新增

- 支付宝官方通道支持支付宝控制台常见的裸 Base64 私钥和公钥格式。

### 修复

- 修复支付通道密钥格式错误时，充值单创建后立即显示“已取消”的问题；现在会在保存配置或创建支付单前返回明确错误，不再生成无效支付单。
- 修复用户端充值失败没有表单内错误提示、按钮没有忙碌状态的问题。

### 安全

- 支付宝私钥和公钥保存前执行 RSA 格式校验，拒绝混入说明文字或其他非密钥内容的凭证。

### 兼容性

- 继续兼容 PKCS#1/PKCS#8 PEM，并新增 PKCS#1/PKCS#8 裸 Base64 私钥与 PKIX/PKCS#1 裸 Base64 公钥。

### 部署提醒

- 升级后请在管理员“支付管理”中重新保存一份干净的支付宝应用私钥和支付宝公钥；私钥内容不能附带“应用公钥”等说明文字。

## [1.8.3] - 2026-08-21

### 新增

- 用户订单支持按目标平台、状态和关键字服务端筛选，并保留统一分页信息。
- 用户门户补充余额充值、资金流水、开发者工具和 Webhook 的可见入口；申请摘要明确展示库存、费用和退款规则。

### 修复

- 修复删除目标平台后服务重启或在线升级又被默认种子恢复的问题；默认平台只在空库首次初始化时创建，已有数据库只记录初始化标记。
- 修复用户端订单筛选控件不生效、刷新后当前任务不继续轮询、任务步骤文案错位和订单操作可重复提交的问题。

### 安全

- 用户门户历史订单列表不再直接展示邮箱地址和验证码，敏感信息仅在用户主动打开详情时显示。
- 复制邮箱增加兼容降级与失败提示，避免剪贴板权限失败时显示错误成功状态。

### 兼容性

- 新增 `seed_states` 表记录一次性业务种子初始化，不修改现有目标平台、订单、余额和邮箱平台状态。
- 用户订单接口保留原有分页参数，`status`、`service` 和 `query` 为可选筛选参数。

### 部署提醒

- 升级前仍需按正式流程创建 PostgreSQL 备份；升级后确认已删除目标平台不会出现在管理员列表、公开服务列表和用户申请页。
- 验证用户端桌面与移动端的申请、当前任务、订单筛选、余额充值和开发者入口。

## [1.8.2] - 2026-08-20

### 新增

- PostgreSQL 启动迁移会幂等补齐所有现有“邮箱 × 目标平台”的缺失状态，并在实际补齐时写入系统审计。

### 修复

- 修复历史邮箱仅存在部分目标平台状态时，邮箱虽然已验证且可用，但对应平台库存显示为零、用户无法申请的问题。

### 安全

- 补齐逻辑使用复合主键冲突保护，只新增缺失状态，不覆盖已有的 `leased`、`consumed` 或其他业务状态。

### 兼容性

- 不修改目标平台价格、邮件匹配、允许邮箱类型、订单和资金数据；迁移可重复执行，完整数据不会产生额外写入。

### 部署提醒

- 升级后检查目标平台库存，并确认 Discord、GitHub、OpenAI 和 Telegram 均能使用已验证的 Outlook.de 邮箱。

## [1.8.1] - 2026-08-20

### 新增

- 验证 Worker 的补偿扫描会自动重新入队已验证但 Graph 有效期未落库或已经过期的邮箱，历史导入数据无需重新上传。

### 修复

- 修复使用现有 Graph Access Token 验证成功时未持久化 `oauth_valid_until`，导致邮箱显示已验证且可用、目标平台库存却始终为零的问题。
- 修复 Graph 首次读取失败、刷新 Token 后重试成功时刷新状态未记录，避免后续逻辑用旧有效期覆盖新凭证有效期。

### 安全

- 只在 Graph 已成功校验账户与收件箱访问后写入有效期；缺少 `expires_at` 时采用 15 分钟保守窗口并周期复验，不在日志或接口中暴露 Token。

### 兼容性

- 不改变邮箱导入格式、目标平台规则、订单状态机和 API 响应；现有 PostgreSQL 与 Redis 数据可直接在线升级。

### 部署提醒

- 升级后等待验证 Worker 处理历史 Graph 邮箱，并确认管理员邮箱池保持已验证状态、用户申请页的目标平台库存恢复为可用数量。

## [1.8.0] - 2026-08-20

### 新增

- 管理后台收敛为运行概览、邮箱池、目标平台、注册订单、用户与余额、支付管理、运维中心和版本升级八个业务入口；资金流水、支付通道和审计记录改为页内标签。
- 注册订单新增服务端状态、目标平台、用户和关键字筛选，列表返回用户邮箱摘要，详情使用侧边抽屉展示失败原因、退款状态和完整时间线。
- 用户余额调整改为增减类型、正数金额、必填原因和调整前后余额预览，成功后直接打开该用户资金流水。
- 版本升级页面新增当前版本到目标版本对照、备份、提交、重启和健康检查进度，以及正式版本日志折叠面板。

### 修复

- 修复管理员切换模块时加载多个无关接口、接口失败后残留上一页面以及历史时间只显示时分秒的问题。
- 修复订单页把当前页统计与全量订单数混用、详情追加在表格底部导致页面跳动的问题。
- 修复手机端订单、用户、资金、支付和审计宽表只能横向滚动的问题，改为摘要卡片和底部详情抽屉。
- 在线升级器在新容器未通过健康检查时尝试恢复上一镜像，并明确记录升级失败或回滚失败状态。

### 安全

- 管理员资金流水增加用户归属字段；余额调整继续在事务内校验余额、写入资金流水和管理审计，不允许扣减为负数。
- 在线升级继续只允许 `ghcr.io/ljunn/heromail:latest`，升级前由应用创建并校验 PostgreSQL 备份，邮箱凭证、支付密钥和用户会话不会进入页面日志。

### 兼容性

- 保留 `/admin/ledger`、`/admin/alerts` 和 `/admin/audit` 旧路由，访问时自动定位到用户、运维页面的对应标签。
- 管理员订单和资金流水接口仅增加可选查询参数及响应字段，不改变现有分页结构、订单状态机、余额结算和支付回调契约。
- 升级器健康检查与自动回滚能力会随新安装包提供；已有部署仍通过网页在线升级切换应用镜像，不需要修改 Compose 镜像地址。

### 部署提醒

- 等待 `v1.8.0` Release 与 `ghcr.io/ljunn/heromail:latest` 多架构镜像完成后，从管理员“版本升级”页面执行在线升级，禁止手动替换生产容器。
- 升级后验证 `/readyz`、管理员登录、订单服务端筛选与详情、用户资金流水、邮箱池和列表分页、申请邮箱以及支付回调配置未丢失。

## [1.7.1] - 2026-08-19

### 修复

- 修复已有 PostgreSQL 数据库升级时写入 `allowed_providers` JSONB 字段的类型错误，避免新容器启动失败并导致服务不可用。

### 安全

- 不改变邮箱凭证、支付配置、余额和订单数据；升级前仍由在线升级器创建 PostgreSQL 备份。

### 兼容性

- 兼容 `v1.6.0` 及 `v1.7.0` 已存在的目标平台和邮箱数据，启动迁移可重复执行。

### 部署提醒

- 生产环境需升级到 `v1.7.1` 后检查 `/readyz`、管理员登录、邮箱列表分页、申请邮箱和支付配置。

## [1.7.0] - 2026-08-19

### 新增

- 将 `@outlook.de` 识别为独立的 `outlook_de` 邮箱类型，并在邮箱池统计、目标平台配置、OAuth 接入和历史数据迁移中保持一致。

### 修复

- 收码 Worker 改为游标分页遍历全部邮箱凭证，避免邮箱超过 100 个后漏收验证码。
- 验证 Worker 每 30 秒补偿扫描待验证邮箱，Redis 短暂入队失败不再留下永久待验证记录。
- Graph 验证优先尝试现有 Access Token，仅在明确过期或 Graph 调用失败后刷新，再失败才回退 IMAP。
- 唯一邮箱池迁移不再删除旧池记录或重置限额配置；旧记录仅停用保留，实际迁移写入系统审计。
- 管理后台移除重复的“收码规则”和“调度策略”入口，目标平台页面集中展示并编辑邮箱类型、邮件匹配、价格和有效期。

### 安全

- 邮箱类型由服务端根据地址统一识别，管理接口不能提交与邮箱域名不一致的供应商类型。

### 兼容性

- 历史 `@outlook.de` 邮箱自动重分类；原先允许 Outlook 的目标平台自动增加 Outlook.de，以保持现有库存可分配。

### 部署提醒

- 升级后检查审计日志中的 `mailbox_pool.consolidate` 记录，并验证 Outlook.de 分类统计、待验证队列和超过 100 个邮箱时的收码轮询。

## [1.6.0] - 2026-08-19

### 新增

- 邮箱资源统一为系统唯一邮箱池，导入 Outlook/Hotmail 账号时自动识别类型，不再要求创建或选择邮箱池。
- 按 Junliai `firefly-gpt-image-2` 视觉稿重做邮箱导入工作台，新增拖放上传、能力说明、全池类型与验证统计以及高密度分页明细。
- 管理概览新增全池邮箱总数、Outlook、Hotmail、待验证和已验证统计字段。

### 修复

- 修复邮箱类型被错误建模为多个邮箱池的问题；启动迁移会将历史邮箱归并到唯一邮箱池，并移除多余池记录。
- 修复邮箱连接方式直接展示内部枚举的问题，现在统一显示 Graph、Graph OAuth、IMAP 或自动判定。
- 修复分页页面使用当前页邮箱数量冒充全池类型和验证统计的问题。

### 安全

- 关闭邮箱池创建与删除管理路由，防止绕过页面重新产生多个资源池。
- 导入仍按行流式处理，错误摘要不包含密码、Token、完整邮件或原始上传内容。

### 兼容性

- 保留现有邮箱记录及其订单、平台注册状态和加密凭证；仅将历史 `pool` 字段归并到系统唯一邮箱池。
- 邮箱导入接口不再需要 `pool` 查询参数，旧客户端携带该参数时会被忽略并导入唯一邮箱池。

### 部署提醒

- 升级启动时会自动归并历史邮箱池，升级前必须确认 PostgreSQL 备份完成。
- 升级后验证 `/readyz`、邮箱导入、Outlook/Hotmail 自动识别、验证队列、列表分页、申请邮箱入口和支付配置未丢失。

## [1.5.1] - 2026-08-19

### 新增

- 无新增功能；本版本用于修正邮箱池两层导航。

### 修复

- 修复管理员点击“邮箱池”后直接进入未筛选邮箱总表的问题；现在未指定邮箱池时只展示邮箱池列表，选择邮箱池后才进入该池的分页邮箱列表与导入界面。

### 安全

- 不改变邮箱凭证、Token、邮件内容和管理员鉴权处理，邮箱数据仍只在管理员权限下展示。

### 兼容性

- 不修改数据库结构、HTTP API、订单、支付或邮箱验证逻辑，仅调整管理员前端路由的展示行为。

### 部署提醒

- 等待 `ghcr.io/ljunn/heromail:latest` 多架构镜像完成后，再从管理员“系统设置”执行在线升级。
- 升级后验证 `/readyz`、邮箱池到邮箱列表的两层导航、列表分页、申请邮箱入口和支付服务商配置。

## [1.5.0] - 2026-08-19

### 新增

- 管理后台将邮箱资源收敛为“邮箱池 -> 邮箱列表”两层视图，邮箱列表支持服务端分页和按池筛选。
- 新增 TXT、CSV、JSON Lines 等常见邮箱记录格式的逐行流式导入，单行限制和错误摘要避免大文件占用内存或泄露凭证。
- 导入邮箱后自动进入 Redis 验证队列；首版支持 Outlook/Hotmail，Graph 优先，Graph 收件失败时回退 IMAP。
- IMAP 连接器支持 OAuth2 和密码登录，并可读取收件箱参与验证码轮询；邮箱默认没有平台注册占用，收码结算后按目标平台标记。

### 修复

- 修复邮箱池聚合列表在 PostgreSQL 下名称、供应商和启用状态为空的问题。
- 修复已通过 IMAP 验证的邮箱仍被 OAuth 有效期条件排除、无法参与订单分配的问题。
- 修复自动验证刷新凭证保存失败时误标记邮箱可用的问题。

### 安全

- 导入和验证错误只返回行号及脱敏原因，不记录密码、Token、完整邮件或上传内容。
- 验证、凭证刷新、导入保存和队列失败继续写入审计日志；管理接口保持 Bearer 会话和管理员权限边界。

### 兼容性

- 保持现有邮箱资产、订单、平台占用、支付和公开服务分页契约；新增字段通过 GORM 自动迁移。
- 继续使用 PostgreSQL、Redis 和 `ghcr.io/ljunn/heromail:latest`，新增 IMAP 依赖仅用于 Outlook/Hotmail 适配器。

### 部署提醒

- 发布后等待官方 Release 对应的 GHCR 多架构镜像完成，再从管理后台“系统设置”使用在线升级；升级前确认 PostgreSQL 备份成功。
- 升级后验证 `/readyz`、登录、邮箱池分页、TXT 导入和自动验证状态、订单分配、验证码轮询及支付回调配置。

## [1.4.0] - 2026-08-19

### 新增

- 邮箱资源新增 Microsoft OAuth2 连接方式、验证状态、最近验证时间、验证错误和已注册平台信息。
- 管理后台新增邮箱连接验证操作，可刷新 OAuth Token、校验 Microsoft Graph 账户并显示失败原因。
- 邮箱列表按服务端分页批量加载已注册平台，避免逐邮箱查询；内存存储和 PostgreSQL 行为保持一致。

### 修复

- 修复同一 Microsoft 邮箱重新授权时因地址唯一约束导致无法更新凭证的问题，现在会复用已有邮箱资产并保留平台占用和活跃订单状态。
- 修复 OAuth 凭证刷新缺少审计记录的问题。

### 安全

- 邮箱连接验证失败时标记为授权异常并降低健康分，不会向用户端暴露凭证、完整邮件或 OAuth Token。
- 邮箱验证、凭证刷新和 OAuth 接入继续写入管理员审计日志。

### 兼容性

- 新增字段通过 GORM 自动迁移，现有邮箱、订单、平台占用和凭证数据保持兼容。
- 保持 Outlook/Hotmail OAuth2 接入、订单分配、退款结算和公开服务 DTO 契约不变。

### 部署提醒

- 发布后等待 `ghcr.io/ljunn/heromail:latest` 多架构镜像完成，再从管理员“系统设置”页面点击在线升级；升级前必须确认 PostgreSQL 备份成功。
- 升级后验证 `/readyz`、登录、邮箱资源验证、服务列表分页、申请邮箱和支付回调配置未丢失。

## [1.3.0] - 2026-08-19

### 新增

- 登录态服务列表按服务端分页返回当前启用平台、单价、有效期、允许邮箱类型和实时可分配余量。
- 新增 `GET /api/v1/services/{code}/availability`，支持按平台代码查询单个平台实时余量，未知或停用平台返回 404。
- 用户门户和内置 API 文档接入新的服务列表与余量契约。

### 修复

- 修复用户服务列表复用管理员 DTO，可能返回停用平台、内部收码规则和平台占用统计的问题。
- 修复余量仅按邮箱平台状态计数、与实际订单分配条件不一致的问题；现在同时检查邮箱状态、活跃租约、健康分、OAuth 有效期、允许渠道、邮箱池、冷却和日限额。

### 安全

- 未登录公开价格接口继续不返回库存；实时余量只允许登录用户或有效 API Key 查询。
- 用户服务接口不返回邮箱地址、内部服务 ID、发件人域名、主题关键词、验证码正则、租约或已消费统计。

### 兼容性

- `GET /api/v1/services` 保持原路径和统一分页结构，但不再返回管理员内部字段；管理功能应继续使用 `GET /api/v1/admin/services`。
- 本版本不新增数据库迁移，不改变订单状态、余额结算、支付回调、Webhook 或邮箱凭证格式。

### 部署提醒

- 发布后等待 `ghcr.io/ljunn/heromail:latest` 多架构镜像完成，再从管理员“系统设置”页面点击在线升级；升级前必须确认 PostgreSQL 备份成功。
- 升级后验证 `/readyz`、登录、服务列表分页、单平台余量、申请邮箱和支付回调配置未丢失。

## [1.2.5] - 2026-08-19

### 新增

- 增加 PostgreSQL 连接串拆分测试，覆盖主机、端口、用户名、密码、数据库名和 SSL 模式传递。

### 修复

- 修复在线升级前备份将完整 PostgreSQL 连接串错误写入 `PGDATABASE`，导致 `pg_dump` 无法连接数据库的问题。

### 安全

- 连接凭证继续只通过子进程环境传递，不进入 `pg_dump` 参数或应用日志。

### 兼容性

- 保持在线升级页面、Release 镜像、PostgreSQL/Redis 数据卷和现有 API 契约不变。

### 部署提醒

- 发布后等待 `ghcr.io/ljunn/heromail:latest` 多架构镜像完成，再从管理员“系统设置”页面点击在线升级。
- 升级后确认升级前备份成功，并验证 `/readyz`、登录、移动端首页、用户门户和管理后台导航。

## [1.2.4] - 2026-08-19

### 新增

- 移动端管理后台新增抽屉式导航，保留用户门户切换入口，避免所有后台菜单同时挤在首屏。
- 移动端用户门户保留申请、任务、订单和账户四个高频入口，并为底部导航预留安全区域。

### 修复

- 修复公开首页移动端演示工作台的浮层、轨道装饰和控制台相互遮挡问题。
- 修复移动端能力指标和文档导航横向拥挤、文字截断的问题。

### 安全

- 本版本仅调整响应式展示和导航交互，不改变认证、权限、邮箱凭证和订单数据边界。

### 兼容性

- 保持桌面端双栏首页、用户门户路由、管理后台路由和现有 API 契约不变。
- 移动端菜单切换通过现有页面导航处理，不新增构建依赖或数据库字段。

### 部署提醒

- 发布后等待 `ghcr.io/ljunn/heromail:latest` 多架构镜像完成，再从管理员“系统设置”页面点击在线升级。
- 升级后验证 `/readyz`、登录、移动端首页、用户门户、管理后台导航和分页列表。

## [1.2.3] - 2026-08-18

### 新增

- 在线升级在创建任务前自动生成并校验 PostgreSQL gzip 备份，备份文件保存到升级持久卷。
- 管理后台明确展示升级前备份步骤，并记录备份完成或失败的审计事件。

### 修复

- 修复在线升级可能在没有最新数据库备份的情况下直接拉取镜像的问题。
- 修复备份失败或升级请求写入失败时仍显示可继续升级的问题，失败状态现在会阻止重复任务。

### 安全

- 数据库连接串通过子进程环境传递，不进入 `pg_dump` 命令参数和应用日志。
- 备份临时文件使用仅所有者可读权限，校验通过后才原子移动到正式文件名。

### 兼容性

- 保持现有在线升级 API、官方 GHCR 镜像约束、PostgreSQL/Redis 数据卷和订单状态不变。
- HeroMail 镜像新增 PostgreSQL 客户端工具，仅用于在线升级前备份。

### 部署提醒

- 升级前请确认 `/upgrade` 持久卷可写；备份失败时系统会拒绝升级，不会拉取或重建容器。
- 发布后仍须等待 GitHub Release 和 `ghcr.io/ljunn/heromail:latest` 多架构镜像完成，再从管理员“系统设置”页面点击在线升级。
- 升级后验证 `/readyz`、登录、申请邮箱、分页列表、支付回调配置和最新备份文件。

## [1.2.2] - 2026-08-18

### 新增

- API 文档按统一的鉴权、服务列表、订单、验证码、Webhook 和错误处理结构重写，并补充真实 `/api/v1` 请求示例。
- 旧服务状态入口改为回到首页，公开官网和用户门户不再展示服务状态菜单。

### 修复

- 修复公开文案使用未经验证的可用率和延迟指标问题，改为展示实际支持的 OAuth2、API 和 HMAC 能力。
- 修正首页、登录页、工作台和管理员目标平台页面的术语，使“申请邮箱、注册任务、验证码、已使用”等概念保持一致。
- 修复旧服务状态页面测试契约与当前网页信息架构不一致的问题。

### 安全

- API 文档明确说明邮箱密码、OAuth Token、完整邮件和内部收码规则不会通过用户接口返回。
- 保持公开服务 DTO 脱敏、Bearer 鉴权、订单预扣与超时退款规则不变。

### 兼容性

- 将全站界面字体切换为 Apple 系统字体栈，并保持现有响应式布局和 API 路径兼容。
- `/status` 保留兼容重定向；数据库结构、环境变量和订单状态没有变化。

### 部署提醒

- 发布或升级前请备份 PostgreSQL；升级后验证 `/readyz`、首页、`/docs`、登录、申请邮箱、分页列表和支付回调配置。
- 正式升级必须等待 GitHub Release 和 `ghcr.io/ljunn/heromail:latest` 多架构镜像发布完成，再从管理员“系统设置”页面点击在线升级。

## [1.2.1] - 2026-08-18

### 新增

- 公开首页新增浅色实时工作台视觉，以浅灰白背景、深蓝正文、薄荷绿状态色和少量琥珀色提示构成更清晰的产品层次。
- 首页任务控制台改为白色工作面板，保留邮箱分配、事件流、验证码结果和安全交接等真实任务状态。

### 修复

- 修复公开首页深色背景与用户偏好的浅色界面不一致的问题，导航、首屏、流程区、数据边界、行动区和页脚现在使用统一浅色体系。
- 修复浅色主题下移动导航、边框、状态文字和辅助面板对比度不足的问题，并保持桌面与移动端无横向溢出。

### 安全

- 浅色主题继续只展示脱敏任务示例，不暴露邮箱密码、OAuth Token、完整收件箱、库存或内部收码规则。
- 未修改认证、权限、支付、订单、邮箱分配和在线升级安全边界。

### 兼容性

- 本版本仅调整公开页面 CSS，不涉及数据库结构、环境变量、订单状态或 API 契约变更。
- 保留 GSAP `3.15.0`、ScrollTrigger、`prefers-reduced-motion` 和现有响应式断点行为。

### 部署提醒

- 发布或升级前请备份 PostgreSQL；升级后请验证公开首页桌面与移动端、`/readyz`、登录和管理后台入口。
- 正式升级必须等待 GitHub Release 和 `ghcr.io/ljunn/heromail:latest` 多架构镜像发布完成，再从管理员“系统设置”页面点击在线升级。

## [1.2.0] - 2026-08-18

### 新增

- 公开首页升级为深色实时控制台视觉，展示目标平台、邮箱分配、邮件事件流和验证码结果等真实任务节点。
- 首页接入固定版本的 GSAP 与 ScrollTrigger，增加首屏时间线、滚动揭示、低频状态动效和桌面指针反馈。
- 动效支持 `prefers-reduced-motion`，用户启用减弱动态效果后不启动循环动画，核心内容保持立即可读。
- 目标平台编辑弹窗新增 Outlook 和 Hotmail 复选框，可明确配置一个或多个允许的邮箱供应商。

### 修复

- 修复目标平台弹窗在小屏幕上超出视口、底部新建按钮无法点击的问题；表单主体现在独立滚动，提交按钮固定可见。
- 修复发件人域名为空时返回“价格、有效期或允许的邮箱供应商无效”的含混提示，现在按单价、有效期、供应商和发件人域名分别返回准确错误。
- 修复目标平台代码、供应商和规则列表没有统一标准化的问题，保存时会去除空白、转为小写并去重。

### 安全

- 服务端将目标平台允许的邮箱供应商限制为 Outlook 和 Hotmail，拒绝未实现适配器的渠道配置。
- 继续要求至少一个发件人域名，避免空规则接收并解析与目标平台注册无关的邮件。
- 公开服务 DTO 和首页预览不暴露邮箱密码、OAuth Token、完整收件箱、库存或内部收码规则。

### 兼容性

- 不涉及数据库结构、环境变量、订单状态或公开 API 响应结构变更；已有合法目标平台配置继续兼容。
- 目标平台代码保存后统一为小写；依赖大小写不同代码的非标准配置需要在升级后检查。
- GSAP 使用固定的 `3.15.0` CDN 资源；资源不可达时页面保留完整静态内容，仅停用增强动效。

### 部署提醒

- 升级前请备份 PostgreSQL；升级后请验证 `/readyz`、登录、目标平台新增弹窗、列表分页、申请邮箱和支付回调配置。
- 正式升级必须等待 GitHub Release 和 `ghcr.io/ljunn/heromail:latest` 多架构镜像发布完成，再从管理员“系统设置”页面点击在线升级。

## [1.1.1] - 2026-08-18

### 修复

- 修复支付宝官方支付无法读取支付宝控制台常见裸 Base64 应用私钥的问题。
- 支付宝应用私钥现在兼容裸 Base64、PKCS#1 PEM 和 PKCS#8 PEM 格式。

### 兼容性

- 不涉及数据库结构、环境变量和支付回调协议变更；现有支付宝 PEM 配置继续兼容。

### 部署提醒

- 升级前请备份 PostgreSQL；升级后请验证 `/readyz`、登录、充值订单创建和支付宝异步回调配置。

## [1.1.0] - 2026-08-18

### 新增

- 新增无需登录即可访问的公开官网、价格页、API 文档、服务状态和开源部署页。
- 新增公开目标平台价格接口，使用服务端分页并仅返回面向用户的脱敏字段。
- 新增独立用户门户入口 `/app/*`，提供申请邮箱、当前任务、订单、余额、开发者和账户导航；移动端增加底部导航。
- 新增独立管理员后台入口 `/admin/*`，保留侧栏和高密度运营视图。
- 统一 HeroMail 品牌标志、favicon、公开页面预览和工作台品牌资产。

### 修复

- 修复用户登录后仍被组织成管理员后台的问题，公开站、用户门户和管理后台现在按路径和角色分流。
- 修复移动端首页产品预览与服务状态信息重叠的问题。
- 修复公开服务列表可能暴露库存、收码规则和内部平台占用字段的问题。

### 安全

- 未登录公开接口不返回邮箱地址、库存数量、发件人域名、主题关键词、验证码正则、凭证或内部状态。
- 普通用户访问 `/admin/*` 会被拒绝并回到用户门户；管理员切换用户门户仍沿用 Bearer 会话权限。

### 兼容性

- 新增公开页面和工作台深层路由，不改变现有登录、订单、支付、Webhook 和管理员 API 契约。
- `GET /api/v1/public/services` 支持统一 `page`、`page_size` 分页参数；原有登录后 `/api/v1/services` 继续返回管理所需字段。
- 不涉及数据库结构变更，旧版本实例可以从 `v1.0.6` 直接在线升级。

### 部署提醒

- 发布前请备份 PostgreSQL；升级仍必须先创建 GitHub Release，再在管理后台检查更新并点击在线升级。
- 升级完成后请访问公开首页、`/pricing`、`/docs`、`/login`、`/app`、`/admin`，并验证 `/readyz`、申请邮箱和分页列表。

## [1.0.6] - 2026-08-18

### 新增

- 支付管理新增服务商编辑和删除操作，并为易支付、支付宝官方提供独立配置弹窗。
- 易支付表单支持 API 地址、PID、PKey 和可选支付宝通道 ID；支付宝官方表单内置官方生产网关，并分别配置 AppID、应用私钥和支付宝公钥。
- 管理员与普通用户均可在账户设置中验证当前密码后修改登录密码，修改成功后自动撤销全部旧会话。
- 重新设计 HeroMail 标志、站点图标、侧栏图标和平台图标，统一使用清晰的线性视觉体系。
- 系统设置新增主动检查 GitHub 正式版本入口。

### 修复

- 修复支付服务商只能新增、无法编辑和删除的问题。
- 修复两种支付协议共用一组含混字段，容易把易支付商户密钥与支付宝 RSA 密钥配置错误的问题。

### 安全

- 编辑支付服务商时不向浏览器返回已有密钥正文，敏感字段留空可保留服务器端加密值。
- 存在待支付订单时禁止删除对应服务商，避免异步回调失去验签配置。
- 修改密码必须校验当前密码，并在同一事务中更新密码、撤销旧会话和签发新会话。
- 在线升级继续要求管理员登录和权限校验，移除重复的独立升级令牌，同时保持固定官方镜像、版本校验和审计记录。

### 兼容性

- 不涉及数据库结构变更；`HEROMAIL_UPDATE_TOKEN` 已废弃，旧环境中残留的变量不会再被读取。
- 支付服务商 API 新增单项详情读取接口，原有列表、保存和删除接口保持兼容。

### 部署提醒

- 已安装 `v1.0.5` 的实例请先备份 PostgreSQL，再通过管理后台在线升级到本版本。
- 升级后请检查支付管理的两类配置弹窗、管理员账户入口、系统设置检查更新和 `/readyz`。

## [1.0.5] - 2026-08-17

### 修复

- 修复 CDN 或浏览器长期缓存旧版 JavaScript，导致升级后管理后台继续运行旧前端逻辑的问题。
- HTML 中的 CSS 和 JavaScript 地址加入提交级版本标识，每个正式版本使用独立缓存键。
- 入口页面禁止缓存，静态资源要求重新验证，确保网页刷新后立即加载当前发布版本。

### 测试

- 新增静态资源版本标识和 HTTP 缓存响应头的回归测试。

### 兼容性

- 不涉及数据库结构、环境变量和 API 变更，与 `v1.0.4` 完全兼容。

### 部署提醒

- 已安装 `v1.0.4` 的实例请通过管理后台在线升级到本版本。
- 升级完成后刷新“系统设置”，按钮应显示“已是最新版本”并处于禁用状态。

## [1.0.4] - 2026-08-17

### 修复

- 修复管理后台版本号带有或不带 `v` 前缀时的比较问题。
- 当前版本与 GitHub 正式版本一致时，升级按钮稳定显示“已是最新版本”并保持禁用。

### 兼容性

- 不涉及数据库结构、环境变量和 API 变更，与 `v1.0.3` 完全兼容。

### 部署提醒

- 已安装 `v1.0.3` 的实例请继续通过管理后台在线升级到本版本。
- 升级完成后请在“系统设置”确认当前版本为 `v1.0.4`，并检查按钮已禁用。

## [1.0.3] - 2026-08-17

### 修复

- 当前版本与 GitHub 最新正式版一致时，管理后台改为显示“已是最新版本”并禁用升级按钮。
- 升级接口新增同版本校验，即使绕过前端也不会重复重建已是最新版的容器。
- GitHub Release 创建改为草稿、资产上传、正式发布三阶段重试，降低 GitHub API `503` 对发布的影响。

### 兼容性

- 不涉及数据库结构和环境变量变更，与 `v1.0.2` 完全兼容。

### 部署提醒

- `v1.0.2` 实例继续使用管理后台在线升级，升级完成后按钮应处于禁用状态。

## [1.0.2] - 2026-08-17

### 修复

- 正式镜像内嵌版本改为对应的 `v*` Release 标签，不再显示为提交 SHA。
- 修复管理后台将已安装正式版本误判为可升级，导致同一版本可重复触发升级的问题。

### 兼容性

- 不涉及数据库结构、环境变量和 API 变更，与 `v1.0.1` 完全兼容。

### 部署提醒

- `v1.0.1` 实例应在管理后台使用在线升级按钮升级，不得手动替换镜像。
- 升级后“当前版本”应显示 `v1.0.2`，并且在已是最新版时禁用升级按钮。

## [1.0.1] - 2026-08-17

### 修复

- 正式发布任务改为独立交叉编译 amd64/arm64 安装包，避免 GitHub Artifact 短时下载故障阻断 Release。

### 发布

- 日常提交仍保留可下载的构建制品，正式 Release 使用同一源码和版本信息现场构建。
- Release 页面继续强制使用本章内容作为正式更新日志。

## [1.0.0] - 2026-08-17

### 新增

- 完成用户注册、登录、会话、角色权限、个人设置和 API Key 管理。
- 完成 PostgreSQL 持久化、Redis 分配锁、事务化邮箱分配和服务端分页。
- 完成邮箱池、Outlook/Hotmail OAuth2、Microsoft Graph 收信、邮件去重和验证码规则匹配。
- 完成注册订单预扣、提交、收码、完成、取消、超时退款和资金流水链路。
- 完成易支付 MD5 签名与支付宝官方 RSA2 支付，支持回调验签和幂等入账。
- 完成 Webhook HMAC 签名、内网地址防护、指数退避、投递记录和手动重试。
- 完成用户端与管理端，覆盖资源、平台、规则、订单、用户、资金、支付、告警和审计。
- 新增 Docker 一键部署、GitHub Actions amd64/arm64 打包、GHCR 多架构镜像和网页在线升级。

### 安全

- 邮箱凭证、OAuth Token、支付私钥和 Webhook 密钥使用 AES-256-GCM 加密存储。
- API Key 和会话令牌仅保存哈希，明文密钥只显示一次。
- 在线升级固定使用 `ghcr.io/ljunn/heromail:latest`，该标签只在 GitHub 正式 Release 中更新。

### 部署提醒

- 首次启动必须配置强管理员密码、64 位十六进制加密主密钥、Worker 令牌和升级令牌。
- Microsoft Graph 和支付通道需要在管理端配置合法的第三方凭证后才会启用。
